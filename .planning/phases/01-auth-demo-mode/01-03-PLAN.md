---
phase: 01-auth-demo-mode
plan: 03
type: execute
wave: 3
depends_on: ["01-01", "01-02"]
files_modified:
  - src/components/ui/DemoBanner.tsx
  - src/components/ui/ProfileMenu.tsx
  - src/components/ui/ConnectSpotifyButton.tsx
  - src/components/ui/WarpTransition.tsx
  - src/components/ui/SessionExpiredBanner.tsx
  - src/app/auth/error/page.tsx
  - src/app/page.tsx
  - src/components/canvas/GalaxyScene.tsx
autonomous: false
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03

must_haves:
  truths:
    - "Unauthenticated visitor sees a neon-styled bottom banner with 'Connect Spotify' call to action"
    - "Clicking 'Connect Spotify' initiates Spotify OAuth flow"
    - "After successful login, a warp transition plays and user sees their profile avatar in the top-right"
    - "Logged-in user can click their avatar and log out"
    - "After logout, warp transition plays and user returns to demo galaxy with banner"
    - "If OAuth fails, user sees a branded error page with 'Try Again' button"
    - "If token refresh fails, a gentle 'Session expired' banner appears"
    - "Session persists across browser tab close and reopen"
  artifacts:
    - path: "src/components/ui/DemoBanner.tsx"
      provides: "Bottom neon banner for unauthenticated visitors with Connect Spotify CTA"
      contains: "Connect Spotify"
    - path: "src/components/ui/ProfileMenu.tsx"
      provides: "Top-right avatar icon with logout dropdown for authenticated users"
      contains: "signOut"
    - path: "src/components/ui/WarpTransition.tsx"
      provides: "Fullscreen fade-to-black transition for login/logout"
      contains: "transitioning"
    - path: "src/app/auth/error/page.tsx"
      provides: "Branded OAuth error page with Try Again button"
      contains: "Try Again"
    - path: "src/components/ui/SessionExpiredBanner.tsx"
      provides: "Gentle reconnect banner when token refresh fails"
      contains: "Session expired"
  key_links:
    - from: "src/components/ui/ConnectSpotifyButton.tsx"
      to: "next-auth/react"
      via: "signIn('spotify') call"
      pattern: 'signIn.*"spotify"'
    - from: "src/components/ui/ProfileMenu.tsx"
      to: "next-auth/react"
      via: "signOut() call"
      pattern: "signOut"
    - from: "src/app/page.tsx"
      to: "src/components/ui/DemoBanner.tsx"
      via: "conditional render when no session"
      pattern: "!session.*DemoBanner"
    - from: "src/app/page.tsx"
      to: "src/components/ui/ProfileMenu.tsx"
      via: "conditional render when session exists"
      pattern: "session.*ProfileMenu"
    - from: "src/components/ui/WarpTransition.tsx"
      to: "src/lib/store.ts"
      via: "reads mode from Zustand store"
      pattern: "useAppStore"
---

<objective>
Wire all auth UI components into the landing page -- demo banner, Connect Spotify button, profile menu, logout, error handling, and warp transitions.

Purpose: Completes the user-facing auth flow. Visitors see the demo galaxy with an inviting "Connect Spotify" banner, can authenticate via OAuth, see a transition effect, and manage their session. This plan delivers all 13 locked decisions (D-01 through D-13) working end-to-end.

Output: Full auth UX -- demo banner, OAuth login, warp transition, profile menu with logout, error page, and session-expired handling.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-auth-demo-mode/01-CONTEXT.md
@.planning/phases/01-auth-demo-mode/01-RESEARCH.md
@.planning/phases/01-auth-demo-mode/01-01-SUMMARY.md
@.planning/phases/01-auth-demo-mode/01-02-SUMMARY.md

<interfaces>
<!-- Key types and contracts from prior plans -->

From src/auth.ts:
```typescript
export const { handlers, auth, signIn, signOut }: NextAuthResult
```

From src/lib/store.ts:
```typescript
type AppMode = "demo" | "authenticated" | "transitioning"
interface AppStore {
  mode: AppMode
  setMode: (mode: AppMode) => void
  user: { name: string; image: string | null } | null
  setUser: (user: AppStore["user"]) => void
}
export const useAppStore: UseBoundStore<StoreApi<AppStore>>
```

From src/components/canvas/GalaxyScene.tsx:
```typescript
interface GalaxySceneProps {
  isAuthenticated: boolean
}
export default function GalaxyScene({ isAuthenticated }: GalaxySceneProps): JSX.Element
```

From src/types/next-auth.d.ts:
```typescript
interface Session {
  user: DefaultSession["user"] & { id: string }
  error?: "RefreshTokenError"
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create all auth UI components (banner, profile menu, warp transition, error page, session-expired banner)</name>
  <files>
    src/components/ui/DemoBanner.tsx,
    src/components/ui/ConnectSpotifyButton.tsx,
    src/components/ui/ProfileMenu.tsx,
    src/components/ui/WarpTransition.tsx,
    src/components/ui/SessionExpiredBanner.tsx,
    src/app/auth/error/page.tsx
  </files>
  <read_first>
    .planning/phases/01-auth-demo-mode/01-CONTEXT.md (D-03, D-04, D-08, D-09, D-10, D-11 for exact UX requirements)
    .planning/phases/01-auth-demo-mode/01-RESEARCH.md (Anti-patterns section, Pitfall 5: layout shift prevention)
    src/lib/store.ts (AppMode type and useAppStore API)
    src/app/globals.css (neon CSS variables available)
  </read_first>
  <action>
    **1. Create `src/components/ui/ConnectSpotifyButton.tsx`** -- "use client" component.
    - Import { signIn } from "next-auth/react"
    - Render a button that calls `signIn("spotify")` on click
    - Style: neon-bordered button with text "Connect Spotify" in the --neon-cyan color
    - Tailwind classes: `px-6 py-2 rounded-full border font-semibold text-sm tracking-wide transition-all hover:scale-105`
    - Use inline style for neon colors: `borderColor: "var(--neon-cyan)"`, `color: "var(--neon-cyan)"`, hover glow effect with `boxShadow`
    - The button has a Spotify icon (use a simple SVG inline -- green circle with 3 arcs, or just text "Connect Spotify" if SVG is complex)

    **2. Create `src/components/ui/DemoBanner.tsx`** -- "use client" component per D-03.
    - Import { motion } from "motion/react" (NOT "framer-motion" -- the package is now "motion")
    - Fixed position at bottom of screen: `fixed bottom-0 left-0 right-0 z-10`
    - Semi-transparent dark background with backdrop blur: `bg-black/60 backdrop-blur-md`
    - Content: "This is a demo galaxy" text + ConnectSpotifyButton
    - Text: "Connect Spotify to see YOUR universe" in a muted white, with "YOUR" in --neon-cyan for emphasis
    - Animate in with motion: fade up from bottom with slight y-offset
    - Per D-03: Minimal, unobtrusive, matches synthwave aesthetic. NOT a modal or popup -- a slim bottom bar.
    - Padding: `py-4 px-6` -- slim, not tall

    **3. Create `src/components/ui/ProfileMenu.tsx`** -- "use client" component per D-10.
    - Import { signOut } from "next-auth/react"
    - Import { useAppStore } from "@/lib/store"
    - Accept props: `user: { name?: string | null; image?: string | null }`
    - Render a small circular avatar in the top-right corner: `fixed top-4 right-4 z-10`
    - If user.image exists, show an img tag with the avatar. Otherwise show a fallback initial letter in a colored circle.
    - On click, toggle a dropdown menu with:
      - User name displayed
      - "Log Out" button that calls handleLogout
    - handleLogout: set store mode to "transitioning", wait 800ms for warp transition, then call signOut({ callbackUrl: "/" })
    - Per D-10: Small profile/avatar icon. Click reveals logout option.
    - Per D-11: After logout, warp transition plays.

    **4. Create `src/components/ui/WarpTransition.tsx`** -- "use client" component per D-04 and D-11.
    - Import { useAppStore } from "@/lib/store"
    - Import { motion, AnimatePresence } from "motion/react"
    - Subscribe to store.mode
    - When mode === "transitioning", render a fullscreen black overlay that fades in quickly (200ms), holds for 600ms, then fades out (200ms)
    - Style: `fixed inset-0 z-50 bg-black` -- covers everything
    - IMPORTANT per RESEARCH.md open question 3: Phase 1 uses a simple fade-to-black transition. The full star-streak warp effect is Phase 4/5. A clean black fade is sufficient.
    - After the transition completes (total ~1000ms), it should auto-hide (AnimatePresence handles this).

    **5. Create `src/components/ui/SessionExpiredBanner.tsx`** -- "use client" component per D-09.
    - Import { useSession, signIn } from "next-auth/react"
    - Check if session?.error === "RefreshTokenError"
    - If so, render a top-center banner: "Session expired -- reconnect" with a "Reconnect" button
    - Button calls signIn("spotify")
    - Style: fixed top-4, centered, neon-styled subtle banner with --neon-pink accent
    - Per D-09: Gentle banner, not an error modal. User can dismiss or click reconnect.

    **6. Create `src/app/auth/error/page.tsx`** -- server component per D-08.
    - This is the custom error page Auth.js redirects to when OAuth fails
    - Dark background matching the app aesthetic (#000005)
    - Centered content: "Something went wrong" heading, brief explanation text, and a "Try Again" button
    - The "Try Again" button is a client component that calls signIn("spotify")
    - Style: neon-themed error page -- NOT a generic white error page. Use --neon-pink for the error accent.
    - The page reads the `error` search param from the URL (Auth.js passes the error type)
    - Display appropriate message based on error type:
      - "OAuthSignin": "Failed to start Spotify login"
      - "OAuthCallback": "Spotify login was denied or failed"
      - "OAuthAccountNotLinked": "This account is already linked to another login"
      - Default: "An unexpected error occurred"
  </action>
  <verify>
    <automated>cd /Users/rafwansarker/AI/projects/spotify-universe && npm run build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/ui/DemoBanner.tsx` contains `"use client"` and `Connect Spotify`
    - `src/components/ui/DemoBanner.tsx` contains `fixed bottom-0` (bottom positioning per D-03)
    - `src/components/ui/DemoBanner.tsx` contains `motion` import from `motion/react`
    - `src/components/ui/ConnectSpotifyButton.tsx` contains `signIn("spotify")`
    - `src/components/ui/ProfileMenu.tsx` contains `signOut` import
    - `src/components/ui/ProfileMenu.tsx` contains `fixed top-4 right-4` (top-right per D-10)
    - `src/components/ui/ProfileMenu.tsx` contains `"transitioning"` (sets mode before logout per D-11)
    - `src/components/ui/WarpTransition.tsx` contains `useAppStore` and `"transitioning"`
    - `src/components/ui/WarpTransition.tsx` contains `AnimatePresence`
    - `src/components/ui/SessionExpiredBanner.tsx` contains `RefreshTokenError` and `signIn("spotify")`
    - `src/app/auth/error/page.tsx` contains `Try Again`
    - `src/app/auth/error/page.tsx` contains `#000005` or `bg-deep` or dark background styling
    - `npm run build` succeeds
  </acceptance_criteria>
  <done>All 6 auth UI components created: DemoBanner with neon styling and Connect Spotify CTA, ProfileMenu with avatar and logout, WarpTransition fade overlay, SessionExpiredBanner for refresh failures, and branded auth error page. Build succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Wire auth UI components into page.tsx and update GalaxyScene for mode awareness</name>
  <files>
    src/app/page.tsx,
    src/components/canvas/GalaxyScene.tsx
  </files>
  <read_first>
    src/app/page.tsx (current state from Plan 02 -- has dynamic GalaxyScene import)
    src/components/canvas/GalaxyScene.tsx (current state from Plan 02 -- Canvas with DemoGalaxy)
    src/components/ui/DemoBanner.tsx (just created -- understand its props)
    src/components/ui/ProfileMenu.tsx (just created -- understand its props)
    src/components/ui/WarpTransition.tsx (just created -- no props, reads from store)
    src/components/ui/SessionExpiredBanner.tsx (just created -- no props, reads from session)
    src/lib/store.ts (AppStore interface)
    src/auth.ts (auth() function)
  </read_first>
  <action>
    **1. Update `src/app/page.tsx`** to include all DOM overlay components.

    The page is an async server component. It checks the auth session and renders:
    - GalaxyScene (dynamic, ssr:false) -- full viewport canvas
    - DemoBanner -- shown when NOT authenticated (bottom banner per D-03)
    - ProfileMenu -- shown when authenticated (top-right avatar per D-10)
    - WarpTransition -- always mounted (reads from Zustand, shows during mode transitions)
    - SessionExpiredBanner -- always mounted (reads from useSession, shows on RefreshTokenError)

    ```typescript
    import dynamic from "next/dynamic"
    import { auth } from "@/auth"
    import { DemoBanner } from "@/components/ui/DemoBanner"
    import { ProfileMenu } from "@/components/ui/ProfileMenu"
    import { WarpTransition } from "@/components/ui/WarpTransition"
    import { SessionExpiredBanner } from "@/components/ui/SessionExpiredBanner"

    const GalaxyScene = dynamic(
      () => import("@/components/canvas/GalaxyScene"),
      { ssr: false }
    )

    export default async function HomePage() {
      const session = await auth()

      return (
        <main className="relative h-screen w-screen overflow-hidden" style={{ background: "#000005" }}>
          <GalaxyScene isAuthenticated={!!session} />

          {/* DOM overlays -- positioned above canvas via z-index */}
          {!session && <DemoBanner />}
          {session?.user && <ProfileMenu user={session.user} />}
          <WarpTransition />
          <SessionExpiredBanner />
        </main>
      )
    }
    ```

    NOTE: DemoBanner, ProfileMenu, WarpTransition, and SessionExpiredBanner are all "use client" components. They can be imported into a server component and rendered -- Next.js handles the boundary. The server component just decides WHICH ones to render based on session.

    **2. Update `src/components/canvas/GalaxyScene.tsx`** for mode awareness.

    Add mode tracking: when mode switches from "demo" to "authenticated" (after Phase 2 adds user galaxy), the scene will swap. For now in Phase 1, the scene always shows the demo galaxy since there's no user data yet. But the architecture must support the transition.

    - Import { useAppStore } from "@/lib/store"
    - Inside the Canvas, use a helper component (e.g., `<SceneContent>`) that reads the store mode inside the R3F context
    - Per Pitfall 3: Read store values with `useAppStore.getState()` inside useFrame, NOT with reactive subscriptions for per-frame values. For discrete mode switches, reactive subscription is fine.
    - The GalaxyScene still accepts `isAuthenticated` prop. On mount, if isAuthenticated is true, set store mode to "authenticated". If false, ensure mode is "demo".
    - Use a useEffect in a client wrapper to sync the prop to the store on mount.
  </action>
  <verify>
    <automated>cd /Users/rafwansarker/AI/projects/spotify-universe && npm run build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/page.tsx` contains `<DemoBanner />` conditionally rendered when `!session`
    - `src/app/page.tsx` contains `<ProfileMenu` conditionally rendered when `session?.user`
    - `src/app/page.tsx` contains `<WarpTransition />`
    - `src/app/page.tsx` contains `<SessionExpiredBanner />`
    - `src/app/page.tsx` contains `import { auth } from "@/auth"`
    - `src/app/page.tsx` contains `style={{ background: "#000005" }}` (instant dark per D-12)
    - `src/components/canvas/GalaxyScene.tsx` contains `useAppStore` import
    - `npm run build` succeeds
  </acceptance_criteria>
  <done>All auth UI components wired into the landing page with conditional rendering based on session state. GalaxyScene is mode-aware via Zustand store. Build succeeds.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify complete auth flow end-to-end</name>
  <files>src/app/page.tsx</files>
  <action>
    Human verification checkpoint. Run `npm run dev` and open http://localhost:3000 in your browser. Follow the verification steps below to confirm the complete Phase 1 auth and demo mode implementation works end-to-end.
  </action>
  <what-built>
    Complete Phase 1 auth and demo mode implementation:
    1. Demo galaxy with ~200 colorful stars auto-orbiting on the landing page
    2. Neon-styled "Connect Spotify" banner at the bottom
    3. Spotify OAuth login flow
    4. Profile avatar with logout in the top-right corner
    5. Warp (fade) transition on login/logout
    6. Branded error page for OAuth failures
    7. Session persistence across browser refresh
  </what-built>
  <how-to-verify>
    **Prerequisites:** You must have set up a Spotify Developer App and filled in `.env.local` with:
    - AUTH_SECRET (run `npx auth secret` to generate)
    - SPOTIFY_CLIENT_ID (from Spotify Developer Dashboard)
    - SPOTIFY_CLIENT_SECRET (from Spotify Developer Dashboard)
    - Redirect URI `http://localhost:3000/api/auth/callback/spotify` registered in Spotify Dashboard

    **Steps:**
    1. Run `npm run dev` in the project root
    2. Open http://localhost:3000 in your browser
    3. Verify: Dark background appears instantly (no white flash) -- per D-12
    4. Verify: ~200 colorful stars are visible, clustered in groups -- per D-05, D-13
    5. Verify: Camera slowly orbits around the galaxy -- per D-02
    6. Verify: You can manually rotate/zoom with mouse -- per D-02
    7. Verify: Bottom banner says "Connect Spotify to see YOUR galaxy" or similar -- per D-03
    8. Click "Connect Spotify"
    9. Verify: Redirects to Spotify login page with correct permissions
    10. Complete the Spotify login
    11. Verify: Warp (fade to black) transition plays after redirect -- per D-04
    12. Verify: Profile avatar appears in top-right corner -- per D-10
    13. Verify: Bottom demo banner is gone
    14. Close the browser tab, reopen http://localhost:3000
    15. Verify: Still logged in (profile avatar visible) -- per AUTH-02
    16. Click avatar, click "Log Out"
    17. Verify: Warp transition plays -- per D-11
    18. Verify: Returns to demo galaxy with bottom banner -- per D-11

    **If Spotify credentials not set up yet:** Verify steps 1-7 only (demo mode). Type "demo-only approved" if demo works but OAuth not yet testable.
  </how-to-verify>
  <verify>
    <automated>cd /Users/rafwansarker/AI/projects/spotify-universe && npm run dev -- --port 3000 &amp; sleep 3 &amp;&amp; curl -s -o /dev/null -w "%{http_code}" http://localhost:3000 &amp;&amp; kill %1</automated>
  </verify>
  <done>User has visually verified the demo galaxy renders, auth flow works end-to-end, and session persists across refresh.</done>
  <resume-signal>Type "approved" if all steps pass. Type "demo-only approved" if demo works but OAuth not tested. Describe any issues otherwise.</resume-signal>
</task>

</tasks>

<verification>
1. `npm run build` succeeds
2. `npm run dev` starts and shows the demo galaxy at localhost:3000
3. Demo banner visible at bottom for unauthenticated visitors
4. Profile menu visible at top-right for authenticated users
5. Warp transition plays on login and logout
6. Auth error page renders at /auth/error with neon styling
7. Session persists across browser refresh
8. All Vitest tests pass: `npx vitest run`
</verification>

<success_criteria>
- Unauthenticated visitors see demo galaxy with neon "Connect Spotify" banner (D-01, D-03)
- Clicking "Connect Spotify" starts Spotify OAuth (AUTH-01)
- After login, warp transition plays and profile avatar appears (D-04, D-10)
- Logout triggers warp transition back to demo (D-11)
- OAuth errors show branded error page with "Try Again" (D-08)
- Token refresh failure shows gentle "Session expired" banner (D-09)
- Session persists across browser refresh (AUTH-02)
- Demo galaxy is view-only (D-06)
- Dark canvas appears immediately on load (D-12)
</success_criteria>

<output>
After completion, create `.planning/phases/01-auth-demo-mode/01-03-SUMMARY.md`
</output>
